$ ->
  # Hide search fields if url specifies repository
  #if $("#repository_header").html() != ""
  #  $("#search_field option[value='" + $('#repository_header').text().replace(': ', '') + "']").attr("selected","selected");

